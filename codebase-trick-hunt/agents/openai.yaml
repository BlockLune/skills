interface:
  display_name: "codebase-trick-hunt"
  short_description: "Hunt a codebase for language-specific, non-obvious coding tricks and report them as concise tips."
policy:
  allow_implicit_invocation: false
